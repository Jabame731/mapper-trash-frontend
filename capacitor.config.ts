import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mapper-frontend',
  webDir: 'www',
  plugins: {},
};

export default config;
